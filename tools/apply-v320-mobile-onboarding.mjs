import fs from 'node:fs';

function replaceOnce(text,oldValue,newValue,label){
  if(!text.includes(oldValue))throw new Error(`${label} anchor not found`);
  return text.replace(oldValue,newValue);
}

let html=fs.readFileSync('index.html','utf8');
html=replaceOnce(html,'<meta name="viewport" content="width=device-width,initial-scale=1">','<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">','viewport');
html=replaceOnce(html,'<title>DEAD DROP // v3.1.1 Loot Priority & Raid Inventory</title>','<title>DEAD DROP // v3.2 Mobile Controls & Raid Guide</title>','title');
html=replaceOnce(html,
`        <button id="raidBtn" class="primary big">현재 RAID 참가</button>\n        <div class="help">창고 → 출격 인벤토리 → 장비 장착 순서입니다. 사망하면 장비창과 출격 인벤토리의 물건을 모두 잃습니다.</div>`,
`        <button id="raidBtn" class="primary big">현재 RAID 참가</button>\n        <details class="raidGuide" open>\n          <summary>처음이라면 · RAID 기본 흐름</summary>\n          <div class="raidGuideGrid">\n            <div><b>1 · 준비</b><span>창고의 총·방어구·탄약을 출격 인벤토리로 옮긴 뒤 장착합니다. 총과 같은 구경의 탄약을 따로 챙겨야 장전할 수 있습니다.</span></div>\n            <div><b>2 · 진입</b><span>RAID는 10분 단위로 열리며 최대 8명의 인간 플레이어가 참가합니다. 빈 자리는 AI가 보충하고 종료 1분 전에는 신규 참가가 막힙니다.</span></div>\n            <div><b>3 · 전투과 루팅</b><span>상자와 시체에서 전리품을 획득합니다. 가방이 꽉 차면 인게임에서 필요 없는 아이템을 버리고 더 좋은 전리품을 챙길 수 있습니다.</span></div>\n            <div><b>4 · 탈출</b><span>탈출구에서 E를 유지해 성공해야 가져온 장비와 전리품을 보존합니다. 전리품은 자동 현금화되지 않으며 로비에서 직접 판매합니다.</span></div>\n          </div>\n          <div class="raidGuideWarn">사망 또는 원정 포기 = 장착 장비 + 휴대품 전부 소실 · 손상된 방탄복과 헬멧은 로비에서 자금을 내고 수리할 수 있습니다.</div>\n          <div class="raidGuideMobile">모바일: 가로모드 권장 · 왼쪽 스틱 이동(끝까지 밀면 달리기) · 오른쪽 스틱 조준(바깥 링까지 밀면 사격) · 화면 버튼으로 장전/상호작용/치료/수류탄/무기 전환.</div>\n        </details>\n        <div class="help">PC: WASD 이동 · SHIFT 달리기 · 마우스 조준/사격 · E 상호작용 · R 장전 · 1/2 무기 전환.</div>`,
'raid guide');
html=replaceOnce(html,
`        <canvas id="game" width="1280" height="720"></canvas>`,
`        <canvas id="game" width="1280" height="720"></canvas>\n        <div id="rotateNotice" class="rotateNotice">모바일은 가로모드로 플레이해주세요.</div>\n        <div id="mobileControls" class="mobileControls" aria-hidden="true">\n          <div id="mobileMoveStick" class="mobileStick moveStick"><div id="mobileMoveKnob" class="mobileStickKnob"></div><span>MOVE · 끝까지 = SPRINT</span></div>\n          <div id="mobileAimStick" class="mobileStick aimStick"><div id="mobileAimKnob" class="mobileStickKnob"></div><span>AIM · 바깥 링 = FIRE</span></div>\n          <div class="mobileActions">\n            <button data-mobile="reload">장전</button>\n            <button data-mobile="interact" class="holdAction">상호</button>\n            <button data-mobile="heal">치료</button>\n            <button data-mobile="grenade">폭탄</button>\n            <button data-mobile="primary">1</button>\n            <button data-mobile="secondary">2</button>\n            <button data-mobile="bag" class="bagAction">가방</button>\n          </div>\n        </div>`,
'mobile controls');
html=replaceOnce(html,'      <aside class="sidePanel">','      <aside class="sidePanel">\n        <button id="mobileBagClose" class="mobileBagClose">가방 닫기 ×</button>','mobile bag close');
html=replaceOnce(html,'<div class="ammoHint">탄약은 구경별로 별도 구매 · T1~T5가 높을수록 피해량 증가</div>','<div class="ammoHint">총과 같은 구경의 탄약을 별도 구매 · T1~T5가 높을수록 실제 피해량 증가 · 빈 탄창은 보유한 호환 탄약 중 높은 티어부터 장전</div>','ammo hint');
html=replaceOnce(html,'<div class="patchNote">v3.1.1 · 상자·시체 우선 상호작용 · 1/2 무기 전환 · RAID 가방 아이템 버리기 · 랜덤 엄폐물</div>','<div class="patchNote">v3.2 · 모바일 전용 트윈스틱 조작 · PC UI 분리 · RAID 진행 설명 강화 · 모바일 가방 패널 · 랜덤 엄폐물</div>','patch note');
fs.writeFileSync('index.html',html);

let css=fs.readFileSync('styles.css','utf8');
css += `\n\n/* v3.2 onboarding + touch-only mobile controls */\n.raidGuide{margin:9px 0 8px;border:1px solid #33434a;background:#0d1418;border-radius:8px;padding:9px 10px}.raidGuide summary{cursor:pointer;font-size:11px;font-weight:900;color:#cbd7dc}.raidGuideGrid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:9px}.raidGuideGrid div{border:1px solid #26343b;background:#0a1013;border-radius:6px;padding:7px}.raidGuideGrid b{display:block;color:#acd1df;font-size:10px;margin-bottom:3px}.raidGuideGrid span,.raidGuideWarn,.raidGuideMobile{font-size:9px;line-height:1.5;color:#8e9ba2}.raidGuideWarn{margin-top:7px;color:#d5a18e}.raidGuideMobile{margin-top:5px;color:#91b9c7}\n.mobileControls,.mobileBagClose,.rotateNotice{display:none}\n@media (hover:none) and (pointer:coarse){\n  html,body{overscroll-behavior:none}.topbar{height:52px;padding:0 10px}.brand{font-size:17px}.sub{display:none}.screen{padding:7px}.lobbyShell{grid-template-columns:1fr}.shopPanel{grid-column:auto}.equipmentPanel,.stashPanel,.shopPanel{min-height:auto}.stashGrid{grid-template-columns:repeat(2,minmax(0,1fr))}.inventoryGrid{grid-template-columns:repeat(3,1fr)}.raidGuideGrid{grid-template-columns:1fr}\n  #raid.screen{padding:0}.raidGrid{display:block;max-width:none;height:calc(100dvh - 52px)}#gameWrap{height:100%;border-radius:0;border-left:0;border-right:0;touch-action:none;user-select:none;-webkit-user-select:none}#game{width:100%;height:100%;object-fit:contain;touch-action:none}.hint{display:none}.topLeft{left:7px;top:7px;gap:4px}.topRight{right:7px;top:7px}.hudBox{min-width:104px;padding:5px 7px}.weaponHud{min-width:145px}.weaponHud b{font-size:16px}.timer{top:7px}.mapHud{top:39px}.visionHud{top:42px}.sprintHud{top:59px}\n  .mobileControls{display:block;position:absolute;inset:0;z-index:18;pointer-events:none;touch-action:none}.mobileStick{position:absolute;bottom:max(16px,env(safe-area-inset-bottom));width:126px;height:126px;border-radius:50%;border:2px solid rgba(185,221,232,.28);background:radial-gradient(circle,rgba(23,43,52,.22),rgba(5,10,13,.42));box-shadow:inset 0 0 20px rgba(94,176,202,.08);pointer-events:auto;touch-action:none}.mobileStick:after{content:"";position:absolute;inset:31%;border:1px solid rgba(185,221,232,.18);border-radius:50%}.mobileStick span{position:absolute;left:50%;bottom:-16px;transform:translateX(-50%);white-space:nowrap;font-size:7px;color:rgba(203,227,233,.62);text-shadow:0 1px 2px #000}.moveStick{left:max(18px,env(safe-area-inset-left))}.aimStick{right:max(18px,env(safe-area-inset-right))}.mobileStickKnob{position:absolute;left:50%;top:50%;width:48px;height:48px;border-radius:50%;transform:translate(-50%,-50%);background:rgba(123,190,211,.28);border:2px solid rgba(181,228,241,.55);box-shadow:0 0 13px rgba(81,182,213,.18);pointer-events:none;will-change:transform}\n  .mobileActions{position:absolute;right:max(158px,calc(env(safe-area-inset-right) + 158px));bottom:max(18px,env(safe-area-inset-bottom));display:grid;grid-template-columns:repeat(3,48px);gap:6px;pointer-events:auto}.mobileActions button{width:48px;height:42px;padding:0;font-size:10px;font-weight:900;background:rgba(17,28,34,.82);border-color:rgba(111,158,177,.65);touch-action:none}.mobileActions .holdAction{border-color:#9b8253}.mobileActions .bagAction{border-color:#6c8b72}\n  .sidePanel{position:fixed;right:7px;top:59px;bottom:7px;width:min(350px,82vw);display:block;overflow:auto;z-index:45;background:rgba(7,10,12,.97);padding:7px;border:1px solid #33434a;border-radius:9px;transform:translateX(calc(100% + 18px));transition:transform .16s ease;box-shadow:-15px 0 35px rgba(0,0,0,.45)}#raid.mobileBagOpen .sidePanel{transform:translateX(0)}.mobileBagClose{display:block;width:100%;margin:0 0 7px;padding:8px;background:#24323a}.sidePanel .panel{margin-bottom:7px}.lootPanel{left:50%;right:auto;top:72px;transform:translateX(-50%);width:min(420px,90vw);max-height:55vh}.openProgress{bottom:32px}.resultCard{width:min(390px,88vw)}\n}\n@media (hover:none) and (pointer:coarse) and (max-width:760px){.mobileStick{width:108px;height:108px}.mobileActions{right:max(134px,calc(env(safe-area-inset-right) + 134px));grid-template-columns:repeat(3,43px);gap:4px}.mobileActions button{width:43px;height:38px;font-size:9px}.hudBox{min-width:92px}.weaponHud{min-width:128px}}\n@media (hover:none) and (pointer:coarse) and (orientation:portrait){.rotateNotice{display:flex;position:absolute;inset:0;z-index:70;align-items:center;justify-content:center;text-align:center;padding:24px;background:rgba(3,6,8,.94);font-size:14px;font-weight:900;color:#dce7eb}.mobileControls{opacity:.18;pointer-events:none}}\n`;
fs.writeFileSync('styles.css',css);

let main=fs.readFileSync('src/main.js','utf8');
main=replaceOnce(main,
`const $=s=>document.querySelector(s),canvas=$('#game'),ctx=canvas.getContext('2d',{alpha:false,desynchronized:true})||canvas.getContext('2d');`,
`const $=s=>document.querySelector(s),canvas=$('#game'),ctx=canvas.getContext('2d',{alpha:false,desynchronized:true})||canvas.getContext('2d');\nconst TOUCH_MODE=matchMedia('(pointer: coarse)').matches||navigator.maxTouchPoints>0;\nlet touchMove={x:0,y:0,sprint:false},touchAim={x:1,y:0},touchInteract=false,touchReload=false;`,
'mobile state');
main=replaceOnce(main,
`const weaponDesc={pistol:'기동성 좋은 저가 단발',smg:'근거리 고연사',ar:'중거리 범용',shotgun:'실내 근접전',dmr:'장거리 고화력'};`,
`const weaponDesc={pistol:'저렴한 반자동 보조무기',smg:'근거리 압박용 고연사 기관단총',ar:'근·중거리 범용 돌격소총',shotgun:'실내와 코너전에 강한 근접 화력',dmr:'중·장거리 정밀 사격용 고화력 소총'};`,
'weapon descriptions');
const oldItemMeta="function itemMeta(it){if(!it)return'';if(it.kind==='weapon'){const w=weaponSpec(it.type);return`${weaponDesc[it.type]} · ${WEAPONS[it.type].caliber} · DMG ${w.damage} · 탄창 ${it.ammo??0}/${WEAPONS[it.type].mag} · T${it.ammoTier||1}`};if(it.kind==='ammo'){const t=AMMO_TIERS[it.tier||1];return`${AMMO_CALIBERS[it.caliber]?.name||it.caliber} · T${it.tier||1} · ${it.qty||0}발 · 피해 ×${(t?.damageMult||1).toFixed(2)}`};if(it.kind==='armor')return`방탄 ${Math.round(it.armor??it.maxArmor??0)}/${it.maxArmor||0}`;if(it.kind==='helmet')return`내구 ${Math.round(it.durability??it.maxDurability??0)}/${it.maxDurability||0}`;if(it.kind==='backpack')return`가방 ${it.capacity||8}칸`;if(it.kind==='med')return`회복 +${it.heal}`;if(it.kind==='grenade')return`폭발 반경 ${it.radius}`;return`가치 ${money(it.value)}`}";
const newItemMeta="function itemMeta(it){if(!it)return'';if(it.kind==='weapon'){const w=weaponSpec(it.type);return`${weaponDesc[it.type]} · ${AMMO_CALIBERS[WEAPONS[it.type].caliber]?.name||WEAPONS[it.type].caliber} · 기본 DMG ${w.damage} · ${w.rpm} RPM · 탄창 ${it.ammo??0}/${WEAPONS[it.type].mag} · 현재 T${it.ammoTier||1}`};if(it.kind==='ammo'){const t=AMMO_TIERS[it.tier||1];return`${AMMO_CALIBERS[it.caliber]?.name||it.caliber} · T${it.tier||1} · ${it.qty||0}발 · 해당 탄 사용 시 무기 피해 ×${(t?.damageMult||1).toFixed(2)}`};if(it.kind==='armor')return`몸통 피격 피해 일부 흡수 · 방탄 ${Math.round(it.armor??it.maxArmor??0)}/${it.maxArmor||0} · 손상 시 로비에서 수리`;if(it.kind==='helmet')return`머리 피격 피해 일부 흡수 · 내구 ${Math.round(it.durability??it.maxDurability??0)}/${it.maxDurability||0} · 손상 시 로비에서 수리`;if(it.kind==='backpack')return`휴대 가능 가방 슬롯을 ${it.capacity||8}칸으로 확장`;if(it.kind==='med')return`H키 · 2초 사용 · HP +${it.heal} · 사용 중 사격 불가`;if(it.kind==='grenade')return`G키 · 폭발 피해 ${it.damage||92} · 반경 ${it.radius||165}`;if(it.kind==='valuable')return`탈출 후 판매하는 전리품 · 평가 ${money(it.value)}`;return`평가 가치 ${money(it.value)}`}";
main=replaceOnce(main,oldItemMeta,newItemMeta,'item descriptions');
main=replaceOnce(main,"...['armor_light','armor_heavy','helmet','backpack','med','grenade'].map(k=>({name:GEAR[k].name,price:GEAR[k].price,meta:'',make:()=>makeGear(k)}))","...['armor_light','armor_heavy','helmet','backpack','med','grenade'].map(k=>({name:GEAR[k].name,price:GEAR[k].price,meta:itemMeta(GEAR[k]),make:()=>makeGear(k)}))",'shop gear descriptions');
main=replaceOnce(main,"if(e.type==='loot')toast(`획득: ${e.item}`);if(e.type==='equip')toast(`장착: ${e.item}`);","if(e.type==='loot')toast(`획득: ${e.item}`);if(e.type==='discard')toast(`버림: ${e.item}`);if(e.type==='equip')toast(`장착: ${e.item}`);",'discard toast');
const oldCrosshair="function drawCrosshair(){if(!session)return;ctx.save();ctx.translate(mouse.x,mouse.y);ctx.strokeStyle='rgba(225,242,247,.92)';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(0,0,7,0,Math.PI*2);ctx.moveTo(-13,0);ctx.lineTo(-5,0);ctx.moveTo(13,0);ctx.lineTo(5,0);ctx.moveTo(0,-13);ctx.lineTo(0,-5);ctx.moveTo(0,13);ctx.lineTo(0,5);ctx.stroke();ctx.restore()}";
const newCrosshair="function drawCrosshair(){if(!session)return;let cx=mouse.x,cy=mouse.y;if(TOUCH_MODE&&snap?.player){const cam=camera(),p=visualPlayer()||snap.player;cx=p.x-cam.x+touchAim.x*150;cy=p.y-cam.y+touchAim.y*150}ctx.save();ctx.translate(cx,cy);ctx.strokeStyle='rgba(225,242,247,.92)';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(0,0,7,0,Math.PI*2);ctx.moveTo(-13,0);ctx.lineTo(-5,0);ctx.moveTo(13,0);ctx.lineTo(5,0);ctx.moveTo(0,-13);ctx.lineTo(0,-5);ctx.moveTo(0,13);ctx.lineTo(0,5);ctx.stroke();ctx.restore()}";
main=replaceOnce(main,oldCrosshair,newCrosshair,'touch crosshair');
const oldInput="function updateInput(){if(!session||!snap?.player)return;let x=0,y=0;if(keys.has('w'))y--;if(keys.has('s'))y++;if(keys.has('a'))x--;if(keys.has('d'))x++;const cam=camera(),wx=mouse.x+cam.x,wy=mouse.y+cam.y,dx=wx-snap.player.x,dy=wy-snap.player.y,l=Math.hypot(dx,dy)||1;input={...input,seq:input.seq+1,moveX:x,moveY:y,aimX:dx/l,aimY:dy/l,sprint:keys.has('shift'),interact:keys.has('e'),reload:keys.has('r')};session.sendInput(input);input.reload=false}";
const newInput="function updateInput(){if(!session||!snap?.player)return;let x=touchMove.x,y=touchMove.y;if(keys.has('w'))y--;if(keys.has('s'))y++;if(keys.has('a'))x--;if(keys.has('d'))x++;let aimX=touchAim.x,aimY=touchAim.y;if(!TOUCH_MODE){const cam=camera(),wx=mouse.x+cam.x,wy=mouse.y+cam.y,dx=wx-snap.player.x,dy=wy-snap.player.y,l=Math.hypot(dx,dy)||1;aimX=dx/l;aimY=dy/l}input={...input,seq:input.seq+1,moveX:x,moveY:y,aimX,aimY,sprint:keys.has('shift')||touchMove.sprint,interact:keys.has('e')||touchInteract,reload:keys.has('r')||touchReload};session.sendInput(input);touchReload=false;input.reload=false}";
main=replaceOnce(main,oldInput,newInput,'touch input');
main=replaceOnce(main,"canvas.addEventListener('mousedown',e=>{if(e.button===0){","canvas.addEventListener('mousedown',e=>{if(TOUCH_MODE)return;if(e.button===0){",'pointer lock desktop only');
main=replaceOnce(main,"$('#lobby').classList.add('hidden');$('#raid').classList.remove('hidden');lastResultHandled=false;","$('#lobby').classList.add('hidden');$('#raid').classList.remove('hidden');$('#raid').classList.remove('mobileBagOpen');lastResultHandled=false;",'close mobile bag on enter');
main=replaceOnce(main,"const p=visualPlayer()||snap.player,scale=.4,sx=(p.x-cam.x)*scale,sy=(p.y-cam.y)*scale,level=p.visionLevel;","const p=visualPlayer()||snap.player,scale=TOUCH_MODE?.30:.4,sx=(p.x-cam.x)*scale,sy=(p.y-cam.y)*scale,level=p.visionLevel;",'mobile fog scale');
main=main.replace("ctx.shadowBlur=8;ctx.beginPath();ctx.moveTo(x-Math.cos(a)*30,y-Math.sin(a)*30);","ctx.shadowBlur=TOUCH_MODE?0:8;ctx.beginPath();ctx.moveTo(x-Math.cos(a)*30,y-Math.sin(a)*30);");
main=main.replace("ctx.shadowBlur=f.self?18:13;","ctx.shadowBlur=TOUCH_MODE?0:(f.self?18:13);");
const initAnchor="ensureBasicPistol();setupRealtime();renderLobby();renderRoomStatus();requestAnimationFrame(draw);";
const mobileSetup=`function setupMobileControls(){\n const root=$('#mobileControls');if(!TOUCH_MODE||!root)return;root.setAttribute('aria-hidden','false');\n const bindStick=(el,knob,onMove,onEnd)=>{if(!el||!knob)return;let pointerId=null;const update=e=>{const r=el.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,max=Math.min(r.width,r.height)*.34;let dx=e.clientX-cx,dy=e.clientY-cy,d=Math.hypot(dx,dy),mag=Math.min(1,d/max);if(d>max){dx=dx/d*max;dy=dy/d*max;d=max}knob.style.transform=\\`translate(calc(-50% + \\${dx.toFixed(1)}px),calc(-50% + \\${dy.toFixed(1)}px))\\`;onMove(d>0?dx/d:0,d>0?dy/d:0,mag)};const end=e=>{if(pointerId!==null&&e.pointerId!==pointerId)return;pointerId=null;knob.style.transform='translate(-50%,-50%)';onEnd?.()};el.addEventListener('pointerdown',e=>{e.preventDefault();e.stopPropagation();pointerId=e.pointerId;try{el.setPointerCapture(pointerId)}catch{}update(e)});el.addEventListener('pointermove',e=>{if(e.pointerId!==pointerId)return;e.preventDefault();update(e)});el.addEventListener('pointerup',end);el.addEventListener('pointercancel',end);el.addEventListener('lostpointercapture',e=>{if(pointerId===e.pointerId)end(e)})};\n bindStick($('#mobileMoveStick'),$('#mobileMoveKnob'),(x,y,mag)=>{if(mag<.14){touchMove={x:0,y:0,sprint:false};return}touchMove={x,y,sprint:mag>.82}},()=>{touchMove={x:0,y:0,sprint:false}});\n bindStick($('#mobileAimStick'),$('#mobileAimKnob'),(x,y,mag)=>{if(mag>.10){touchAim={x,y}}input.shoot=mag>.72},()=>{input.shoot=false});\n const press=(name,fn)=>{const b=root.querySelector(\\`[data-mobile="\\${name}"]\\`);if(!b)return;b.addEventListener('pointerdown',e=>{e.preventDefault();e.stopPropagation();fn(e)})};\n press('reload',()=>{touchReload=true});press('grenade',()=>session?.sendAction({type:ACTIONS.GRENADE}));press('heal',()=>session?.sendAction({type:ACTIONS.USE_MED}));press('primary',()=>session?.sendAction({type:ACTIONS.SWAP_WEAPON,slot:'primary'}));press('secondary',()=>session?.sendAction({type:ACTIONS.SWAP_WEAPON,slot:'secondary'}));press('bag',()=>$('#raid').classList.toggle('mobileBagOpen'));\n const interact=root.querySelector('[data-mobile="interact"]');if(interact){const stop=e=>{e?.preventDefault?.();touchInteract=false};interact.addEventListener('pointerdown',e=>{e.preventDefault();e.stopPropagation();touchInteract=true;try{interact.setPointerCapture(e.pointerId)}catch{}});interact.addEventListener('pointerup',stop);interact.addEventListener('pointercancel',stop);interact.addEventListener('lostpointercapture',stop)}\n $('#mobileBagClose')?.addEventListener('pointerdown',e=>{e.preventDefault();$('#raid').classList.remove('mobileBagOpen')});\n window.addEventListener('blur',()=>{touchMove={x:0,y:0,sprint:false};touchInteract=false;touchReload=false;input.shoot=false});\n}\nsetupMobileControls();\n`;
main=replaceOnce(main,initAnchor,mobileSetup+initAnchor,'mobile setup');
fs.writeFileSync('src/main.js',main);
console.log('v3.2 mobile controls and onboarding applied');
